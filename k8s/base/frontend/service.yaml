apiVersion: v1
kind: Service
metadata:
  name: waifu-frontend
  labels:
    app: waifu-animation
    component: frontend
spec:
  selector:
    app: waifu-animation
    component: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
