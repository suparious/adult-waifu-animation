# Core dependencies with Python 3.12 compatible versions
setuptools>=69.0.0
wheel>=0.41.0

# Application dependencies
fastapi==0.128.0
uvicorn[standard]==0.40.0
websockets==12.0
httpx==0.25.1
pydantic==2.4.2
python-multipart==0.0.22
python-dotenv==1.0.0
numpy==2.4.1  # Updated for Python 3.12 compatibility

# Auth and Memory feature dependencies (Issues #2 and #7)
secrets  # Standard library, but ensure available
